# `next-window`: 次のウィンドウ (`[prefix]` + `Space`)

    next-window [-a] [-t target-session]
    (alias: next)

Move to the next window in the session.  
If `-a` is used,
move to the next window with an alert.
