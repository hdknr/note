# `last-pane`: 前回のペインを選択する  (`[prefix]` + `;`)

    last-pane [-de] [-t target-window]
    (alias: lastp)

Select the last (previously selected) pane.  
`-e` enables or `-d` disables input to the pane.
