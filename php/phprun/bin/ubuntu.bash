#!/bin/bash
PKGS=(
    build-essential
    curl
    libbz2-dev
    libreadline-dev
    libsqlite3-dev
    libssl-dev
    llvm
    make
    wget
    zlib1g-dev
    libmysqlclient-dev
    mysql-server
    pkg-config
    graphviz
    graphviz-dev
    libgraphviz-dev
    unzip
    libfreetype6
    libfreetype6-dev
    supervisor
    memcached
    libmemcached-dev
    libmemcached-tools
    nginx
    re2c
    libzip-dev
    libxml2-dev
    # libcurl4-gnutls-dev
    libjpeg9-dev
    libmcrypt-dev
    libtidy-dev
    libxslt1-dev
    libmcrypt-dev
    autoconf
    libsodium-dev
    bison
    curl
    php-cli
    libcurl4-openssl-dev
    software-properties-common
)
sudo apt-get update && sudo apt-get install -y  "${PKGS[@]}"
